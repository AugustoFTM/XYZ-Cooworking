// Função que adiciona ou remove a classe 'rotate' no container
// Isso ativa a rotação 3D entre o formulário de login e cadastro
function toggleForm() {
    const container = document.getElementById("container");
    container.classList.toggle("rotate");
}